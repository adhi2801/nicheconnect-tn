fastapi==0.141.1
# Pinned although FastAPI pulls it in: FastAPI only asks for >=0.46.0, and
# advisories in that range stay open until 1.3.1. Left unpinned, a fresh
# install could resolve to a vulnerable Starlette (security.md section 8).
starlette==1.6.0
uvicorn[standard]==0.53.0
sqlalchemy==2.0.54
alembic==1.20.0
psycopg[binary]==3.3.6
pgvector==0.5.0
redis==8.1.0
pydantic-settings==2.15.0
slowapi==0.1.10
PyJWT==2.14.0
pytest==9.1.1
# Starlette's test client runs on httpx2. The original httpx is no longer
# maintained, and Starlette warns until httpx2 is installed (D-047).
httpx2==2.13.0
# Developer tools (D-037). The app never imports them; they are here, pinned,
# so every laptop and CI check the code with the same versions, and so the
# dependency audit covers them too.
ruff==0.16.8
mypy==2.3.1
pytest-cov==7.1.0
# Pinned although pytest-cov pulls it in: unpinned, local and CI runs could
# count branches differently (D-047).
coverage==7.16.1
# API fuzz testing (D-047). Reads docs/api/openapi.json, generates thousands
# of requests from it and checks every answer. 4.27.5 rather than the newest
# 4.28.0: a tool that gates merges gets to settle for a few days first.
schemathesis==4.27.5
# Background jobs (D-060). Durable workflows whose state lives in Postgres, in
# DBOS's own `dbos` schema; they run inside the app, so there is no broker and
# no new service to deploy (CLAUDE.md section 3).
dbos==3.1.0
# Checks the RFC 3161 timestamps on the deal record's daily checkpoint (D-060).
rfc3161-client==1.0.9
# Pinned although rfc3161-client pulls it in: it performs the signature checks,
# so which version does that is ours to choose, not the resolver's.
cryptography==50.0.1
